# WinFinder — 完整验收核对（Phase 0 + 1 + 2，规格 §11）

> 判据（§11.1）：布局几何 ≤1px、核心颜色 ΔE≤2、字体（Segoe UI Variable）一致、
> 图标取系统级一致；各交互态逐状态比对。豁免（§11.3）：Mica/亚克力色调、字体
> 抗锯齿/ClearType、窗口阴影。

## 自动化门槛（已通过）

| 门槛 | 命令 | 结果 |
|---|---|---|
| Rust 单元测试 | `cargo test` | ✅ 22 passed |
| 前端单元测试 | `pnpm test` | ✅ 53 passed |
| 类型检查 | `tsc --noEmit` | ✅ clean |
| 前端构建 | `pnpm run build` | ✅ ok |
| 发布打包 | `pnpm tauri build` | ✅ exe + msi + nsis |
| 运行验证 | 启动 `winfinder.exe` | ✅ 进程存活、无 panic |
| 代码评审 | 3 轮 holistic review | ✅ 所有发现已修复 |

## 功能验收

| 功能 | 验证点 | 状态 |
|---|---|---|
| **列表/导航** | 列目录、面包屑（段点击/同级/编辑）、后退/前进/向上、刷新(F5)、Backspace=后退、主页 | ☐ 手动 |
| **8 种视图** | 超大/大/中/小图标、列表、详细信息、平铺、内容（Ctrl+Shift+1-8 / 下拉 / 状态栏滑块） | ☐ 手动 |
| **详细信息增强** | 列头排序 ▲/▼、**列宽拖拽** | ☐ 手动 |
| **选择** | 单击/Ctrl/Shift/Ctrl+A/**方向键 ↑↓+回车+自动滚动**、状态栏可见计数 | ☐ 手动 |
| **文件操作** | 新建文件夹/文件、重命名、复制、剪切/粘贴（递归/跨卷/同名自动改名）、删除入回收站、永久删除、打开 | ☐ 手动 |
| **撤销/重做** | Ctrl+Z / Ctrl+Y（新建/重命名/复制/移动可逆；删除不可逆-已知限制） | ☐ 手动 |
| **缩略图/图标** | 图片缩略图；文件类型系统图标（或 emoji 回退） | ☐ 手动 |
| **文件监听** | 外部增删改后当前视图自动刷新（去抖、单 watcher 无泄漏） | ☐ 手动 |
| **右键菜单** | Win11 现代：打开/新标签页/新建/剪切/复制/粘贴/重命名/删除/属性/固定到快速访问 + "显示更多选项"占位（Shift+F10） | ☐ 手动 |
| **搜索** | 搜索框（当前文件夹+子目录，递归、大小写不敏感，Ctrl+E/F 聚焦） | ☐ 手动 |
| **属性对话框** | Alt+Enter：类型/位置/大小（文件夹聚合）/日期/属性 | ☐ 手动 |
| **内联重命名** | F2 / 右键 / 命令栏：行内输入（回车提交、Esc 取消、失焦提交） | ☐ 手动 |
| **视图切换** | View▾/Sort▾ 下拉、**View→显示：隐藏项目/文件扩展名** | ☐ 手动 |
| **预览/详情窗格** | Alt+P 预览（缩略图+元数据）/ Alt+Shift+P 详情（元数据），互斥 | ☐ 手动 |
| **主页** | 特殊文件夹卡片 + **最近使用的文件**（打开时记录、持久化） | ☐ 手动 |
| **快速访问** | 右键文件夹 **固定/取消固定**，导航窗格"快速访问"区（持久化） | ☐ 手动 |
| **标签页** | 多标签（每标签独立位置历史）、Ctrl+T 新建 / Ctrl+W 关闭（末标签关窗口）/ Ctrl+Tab 切换 / Ctrl+1-9、中键文件夹=新标签、中键标签=关闭、右键"在新标签页打开" | ☐ 手动 |
| **多窗口** | Ctrl+N 新窗口（各窗口独立状态）/ Ctrl+W 关闭 | ☐ 手动 |
| **拖放** | 拖文件到文件夹（视图内/导航窗格/快速访问）移动，Ctrl=复制；拖入源自身目录为空操作 | ☐ 手动 |
| **命令栏** | 新建/剪切/复制/重命名/删除/粘贴 + View▾/Sort▾ 下拉 | ☐ 手动 |
| **主题/窗口** | 跟随系统深/浅色；自定义 Mica 标题栏；窗口按钮；F11 全屏；Ctrl+L/Alt+D/F4 聚焦地址栏 | ☐ 手动 |

## 已知限制

- 删除不可撤销（回收站还原在 `trash` crate 不可靠）。
- 复制/移动为同步执行，无流式进度条；同名冲突自动改名（无 替换/跳过 弹窗）。
- "显示更多选项"为占位（未接入真实 Shell 经典菜单）。
- 橡皮筋框选未实现。
- 多窗口间 固定/最近 不实时同步（localStorage 仅在重载时重读）。

## 视觉零偏差门槛（§11，需用户截图比对）

与真机 Windows 11 文件资源管理器逐区域并排比对（浅/深主题）。基准采集见
`docs/acceptance/reference/README.md`，数据集见 `docs/acceptance/dataset/README.md`。
任何可见偏差（非 §11.3 豁免）须修复后重比，直至全绿。

> 说明：自动化门槛与功能实现已全部完成（Phase 0+1+2），并由 22+53 测试 / 构建 /
> 运行 / 3 轮代码评审验证。视觉零偏差比对需用户在真机 Win11 上采集基准截图后进行。
