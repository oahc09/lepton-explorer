# Phase 0 界面验收核对（规格 §11）

> 判据（§11.1）：布局几何 ≤1px、核心颜色 ΔE≤2、字体（Segoe UI Variable）一致、
> 图标取系统级一致；各交互态逐状态比对。豁免（§11.3）：Mica/亚克力色调、字体
> 抗锯齿/ClearType、窗口阴影。除豁免外，任何可见差异须修复后重比。

## 自动化部分（已完成）

- [x] 后端编译通过（`cargo build` Finished）。
- [x] 前端类型检查通过（`tsc --noEmit` 无错误）。
- [x] 前端构建通过（`pnpm run build`，59 模块）。
- [x] 全部单元测试通过（`pnpm test`，23 项；`cargo test`，5 项）。
- [x] 应用可启动：`pnpm tauri dev` 启动无 panic，窗口正常打开（lepton-explorer.exe 运行）。

## 视觉比对部分（需用户完成）

按下表采集真机 Win11 基准与 Lepton Explorer 截图并并排比对，逐项填结论。任何"偏差"非空项
须返回对应组件修复后重比，直至全 ✅。

| 区域 | 状态 | 主题 | 偏差 | 结论 |
|---|---|---|---|---|
| 标题栏（Mica、窗口按钮） | 默认 | 浅 | _待填_ | ⬜ |
| 标题栏（关闭按钮 hover） | hover | 浅 | _待填_ | ⬜ |
| 标题栏 | 默认 | 深 | _待填_ | ⬜ |
| 工具栏（后退/前进/向上/刷新） | 全启用 | 浅/深 | _待填_ | ⬜ |
| 工具栏（前进禁用） | 禁用态 | 浅 | _待填_ | ⬜ |
| 面包屑 | 浏览态 | 浅/深 | _待填_ | ⬜ |
| 导航窗格 | 展开"此电脑" | 浅/深 | _待填_ | ⬜ |
| 详细信息视图 | 默认 | 浅/深 | _待填_ | ⬜ |
| 详细信息视图 | 选中 | 浅/深 | _待填_ | ⬜ |
| 大图标视图 | 默认 | 浅/深 | _待填_ | ⬜ |
| 状态栏 | 无选中 | 浅/深 | _待填_ | ⬜ |

## 比对方法（§11.4）

1. 并排人工比对：同区域、同主题、同数据集、同尺寸左右并排，目视找差异（主方法）。
2. 差分叠加（辅助）：对非 Mica 区域用 `pixelmatch` 等计算差异像素占比，要求 <0.5%。
3. 交互态须手动触发后截图，不可只比默认态。

## Phase 0 视觉验收状态

- 自动化门槛：✅ 全部通过（构建、类型、测试、启动）。
- 视觉零偏差门槛：⏳ 待用户截图比对（需真机 Win11 基准，无法自动化）。
